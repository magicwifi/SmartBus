 <% provide(:title, 'All users') %>
    <h1>All users</h1>
    <ul class="users">
<% @trashes.each do |trash| %> 
         <li>
	 <%= gravatar_for trash  %>
	<div class="anode"><%= trash.name %> | <%= trash.phonenum  %></div>
        </li>
<% end %> 
</ul>
